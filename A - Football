#include <bits/stdc++.h>
#define pb push_back
#define ld long double
#define sz size()
#define lulu for(int i=0;i<n;i++)
#define ed endl
#define ct cout

using namespace std;


int main()
{

    int c,d,e,n,f,x,y,z,cnt=0,res=0,mx=1;
    int sum=0;
    bool ash=false;
    string s;
    //cin>>n;
    //int a[n];
    cin>>s;
    n=s.sz;
    lulu
    {
        if(s[i]==s[i+1])
        {
            cnt++;
        }
        else{cnt=0;}
        res=max(cnt,res);
    }
    if(res>=6)
    {
        ct<< "YES"<<ed;
    }
    else
    {
        ct<<"NO"<<ed;
    }





















    return 0;
}

